# AGENTS.md

AI 에이전트(Claude Code · Codex CLI)가 이 저장소에서 일할 때의 규칙. `CLAUDE.md`는 이 파일의 심볼릭 링크다.

## 프로젝트

**라니스타(Lanista)** — 로마 검투사 양성소를 경영하는 자동전투 게임. TypeScript + Vite, 서버 없음, 저장은 브라우저 localStorage. 폼페이 벽 낙서풍 스틱맨(캔버스 직접 그리기)과 합성 효과음(Web Audio, 음원 파일 없음). UI 프레임워크를 쓰지 않는다.

```bash
npx vite              # 웹 (http://localhost:5173, 주소 뒤 #42 로 시드 고정, ?debug 로 디버그 버튼)
#   전투만 보기: ?debug&fight=eques,retiarius:murmillo,thraex#42 — 우리:상대 유형(쉼표, ? 는 무작위). 저장 안 건드림, 결과 화면의 '다시'로 시드 +1
npm run typecheck     # 변경 후 반드시
npm run sim -- 1000   # 봇 전략별 밸런스 시뮬레이션 (둘째 인자 = 시즌 수)
npm run traits:gain   # 편성 특성 기여: 같은 유형 N명 vs 다른 유형 N명 (없음/1단계 고정/그대로)
npm run stat:worth    # 전력식 가중치: 거울 대결에서 스탯 +1 의 승률 이득 (CONFIG.power 의 근거)
npm run growth -- 200 가성비   # 시즌별 성장 곡선: 우리 평균·으뜸·상대 전력, 승률, 능력치 평균
npm run curve -- 4000 1   # 전력 차별 승률 곡선 (둘째 인자 = 팀 인원). 전투 규칙·우연성을 바꾸면 sim 과 함께 본다
npm test              # 골든 테스트 (node:test + tsx, 새 의존성 없음). 규칙을 바꾸면 여기가 먼저 깨진다
npm run play          # 콘솔 대화형 (npm run play -- 42)
npm run build
```

## 구조와 경계

```
src/core/   게임 규칙. 화면 의존성 없음 — 다른 엔진으로 옮길 때 이것만 번역한다
              classes.ts 클래스(주장비+보조장비 특성 = 장비 수치) · dictata.ts 딕타타(주장비·보조장비·유형 규정 동작) · traits.ts 특성(편성에서 1·2·3명 문턱) · classic.ts 전통 짝 · matchup-table.ts 상성표(굽는다)
src/web/    화면 (2026-09-16 리팩터링으로 화면별 모듈로 나눔)
  main.ts        진입점: 무대 배율(fitStage)·저장·초기 상태 대입·render() 분배·서판 토글·탭 바
  state.ts       공유 가변 상태 S (모듈 수준 let 을 한 객체로). 모든 화면 모듈이 S.x 로 읽고 쓴다
  dom.ts         h()·모달(ask/tell)·드롭다운·툴팁·칩 판별   ※ h 는 함수 선언 — 모듈 순환에서 초기화 순서 안전
  header.ts      헤더(이름·월계관·자금·소식·설정)              [Codex: 배치]
  board.ts       여정표·알붐                                      [Codex: 모양]
  town.ts        마을 캔버스 루프·카메라·이동·입력
  scenes.ts      마을 장면 그림(의무실·훈련소·포룸·시장·묘지)·좌표 상수 [Codex: 그림]
  cells.ts       켈라 격자 장면·방 시트
  sheets.ts      시트(시설·독토르·파밀리아·소식·지원자·규칙)·대시보드 항목
  detail.ts      검투사 상세·확정 페이지·카드 조각·상세 스와이프 [Codex: 인터랙션 감각]
  plan.ts        편성·서판·시즌 확정·시즌 진행(시작→경기→정산) [Codex: 카드/벽 시각]
  battle-view.ts 전투 재생 화면 (규칙은 core/battle.ts)        [Codex: 연출/가독성]
  summary.ts     정산·후계·종료
  portrait.ts    스틱맨 초상 캔버스·대화 장면 루프           [Codex: 그림]
  stickman.ts    그림 리그 · loadout.ts 장비 그림 · sound.ts 효과음
  style.css      전부 Codex 담당
src/cli/    콘솔 실행기(play)와 봇 시뮬레이션(bots·sim)
docs/       01 사전조사 → 02 컨셉 → 03 시대 배경 → 04 예명 → 05 숫자 → 06 프로토타입 기록 → 07 플레이테스트
```

**깨면 안 되는 선**

1. `src/core/`에 `document`·`window`·DOM·캔버스를 들이지 않는다. 지금 0건이고 유지한다.
2. **난수는 `core/rng.ts`의 `Rng`만.** core에서 `Math.random()` 금지 — 시드 재현이 깨지면 시뮬레이션과 버그 재현이 무의미해진다. `src/web/`의 피 튀김·먼지 같은 순수 연출과 새 게임 시드 뽑기에만 `Math.random()`이 허용된다.
3. **전투는 순수 함수다.** `battle.ts`가 0.1초 틱으로 끝까지 돌려 프레임·이벤트를 만들고, 화면은 그것을 재생만 한다. 화면에서 판정하지 않는다.
4. import는 `.js` 확장자를 붙인다 (`'./types.js'`). Bundler 해석이지만 관례가 일관되어 있다.
5. 의존성을 늘리지 않는다. 런타임 의존성 0개가 기본값이고, 아이콘(Lucide)처럼 이미 있는 것 외에 추가하려면 먼저 묻는다.

## 수치는 `core/config.ts` 한 곳에만

모든 밸런스 상수는 `CONFIG`에 있다. 규칙 파일에 숫자를 하드코딩하지 않는다. 상수를 새로 만들 때는 **그 줄에 한국어 주석으로 의도·근거를 남긴다** — 기존 항목들이 전부 그렇게 되어 있고, 이 주석이 밸런스 기록의 1차 사료다.

`config.ts`를 고쳤으면 `docs/05-numbers.md`를 같이 고친다 (파일 첫 줄에 명시된 약속).

## 밸런스를 고칠 때

1. `npm run sim -- 1000`로 **먼저** 현재 수치를 측정한다.
2. `config.ts` 수정.
3. 다시 측정하고 파산율·평균자금·출전당사망·승률·완주시 검투사수를 비교한다.
4. 숫자만 맞추지 말 것. 과거 실패 사례가 `docs/06-prototype.md`에 회차별로 남아 있다 — 시작 자금이 5배로 불어난 1차, 배상금이 커서 "죽이는 게 본업"이 된 문제, 대여료가 승패 무관이라 지는 게 이득이던 문제. 같은 함정을 다시 밟지 않도록 06을 먼저 읽는다.
5. 아직 알려진 미해결 항목은 `docs/07-playtest.md`에 번호로 정리되어 있다. 밸런스 작업은 대체로 이 목록에서 고른다.

## 문서를 어디에 쓰는가

- `docs/05-numbers.md` — 수치의 현재 값과 **근거**(칙령 비율, 고증, 시뮬 결과). 표 형식.
- `docs/06-prototype.md` — 회차별 변경 기록. 무엇을 왜 바꿨고 시뮬 결과가 어떻게 달라졌는지. 날짜를 적는다.
- `docs/07-playtest.md` — 실제로 플레이해 본 메모. 부족한 점을 번호로, 후보 해법과 함께.
- `01`~`04`는 사전조사·컨셉·시대 배경·예명 풀로, 설계의 전제다. 고치기보다 **읽고 따른다.**

규칙이나 밸런스를 바꾼 커밋은 문서 갱신을 같이 하거나, 바로 뒤에 `문서:` 커밋으로 남긴다.

## 고증

용어·규칙·이름은 가능한 한 실제 기록(폼페이 낙서, 묘비, 문헌, 디오클레티아누스 최고가격령, 울피아누스 생명표)을 따른다. 게임 편의와 고증이 부딪히면 **고증을 기본값으로 두고, 벗어날 때 주석에 이유를 남긴다.** 라틴어 용어는 한국어 음차로 쓴다(티로, 베테라누스, 미시오, 루디스, 켈라, 독토르, 파밀리아).

## 상성표

`src/core/matchup-table.ts` 는 **손으로 고치지 않는다.** 전력이 같은 유형 짝을 실제로 붙여 잰 승률표이고, `npm run matchup:table` 이 다시 구워 덮어쓴다. 전투 규칙(`battle.ts`·장비·`config.ts`)을 고쳤으면 다시 굽는다 — 안 그러면 화면의 전력이 옛 규칙을 말한다. 이 표는 카드의 전력 숫자(`powerNow`)에 상성으로 실린다.

## 화면 확인

고친 화면은 **사용자가 에고 브라우저에서 직접 본다.** `npm run dev` 서버가 늘 떠 있다. 에이전트는 `npm run typecheck` · `npm test` 까지만 하고, 무엇이 어떻게 보일지 말로 설명한 뒤 넘긴다. **chrome-devtools 로 크롬 탭을 띄우지 않는다** (2026-09-17 사용자). 픽셀 단위 판단에 스크린샷이 꼭 필요하면 먼저 묻는다.

## 검투사 카드

검투사를 보여 주는 카드는 **`src/web/gcard.ts` 한 종류**다 (2026-09-17 사용자: "카드는 모두 통일"). `gladCardParts` 는 줄 셋만(이미 타일 상자가 있는 자리), `gladCard` 는 상자까지. 화면마다 다른 것은 `nameExtra`(이름 뒤 표식) · `rows`(덧붙는 줄) · `acts`(행동 버튼) · `cls`(`full`·`small`) 로만 준다. 새 카드를 손으로 짜지 않는다.

## 효과음

**효과음은 그 일이 화면에 보일 때만 낸다 (2026-09-17 사용자 규칙).** 소리는 지금 보고 있는 것을 설명해야 한다 — 보이지 않는 곳의 소리는 정체를 알 수 없어 거슬린다.

- 마을: 장면은 하나의 긴 캔버스이고 카메라가 옮겨 다닌다. `S.view`(카메라가 향하는 장소)로 거르지 말고 **그 물체의 실제 x 가 카메라 창 안인지**로 판단한다. 예: 훈련소 채찍은 `TOWN.yardX + 채찍끝.x` 가 `[S.camX, S.camX + S.VW]` 안일 때만 울린다.
- 전투(2026-09-18 유형 정리, docs/09): 유형 열. `battle.ts` 는 능력치·속도·접근 방식·특성·계보 + 장비 수치 둘(사거리·막기, `classes.ts`) + 딕타타 셋(`dictata.ts`)을 읽는다. 투구·받아넘기기·닳음·다리·무기 놓침·기술은 없다(기술은 별개 개념으로 나중에). `audible(x)` 로 카메라 밖 타격 소리를 막는다(`battle-view.ts`). 3대3 에서 화면 밖 싸움의 소리가 새지 않게.
- 다른 앱을 보는 중이면 내지 않는다 (`document.visibilityState`).
- 누름에 대한 반응(버튼·클릭)은 예외다. 누른 사람이 곧 보고 있다.

## 코드 스타일

- 주석·문자열·커밋 메시지는 **한국어**. UI 텍스트도 전부 한국어다.
- 한 줄에 밀도 높게 쓰는 기존 스타일을 따른다. 짧은 함수는 한 줄, 관련 상수는 한 줄에 모으고 줄 끝에 설명 주석을 단다. 새 파일이라고 다른 포맷을 들이지 않는다.
- **줄 끝 주석은 `/* … */` 로.** 한 줄에 문장이 여럿인 코드에 `//` 를 끼우면 뒤 문장이 통째로 주석이 되고, 문법상 유효해서 타입체크가 잡지 못한다 (2026-09-16 하루에 네 번 겪음). 문장 사이에 주석을 넣을 땐 반드시 블록 주석.
- 모듈 순환(render ↔ 화면 모듈)이 있으므로 **모듈 초기화 때 다른 모듈의 `const` 화살표 함수를 부르지 않는다.** 초기화 때 불리는 도우미는 함수 선언으로.
- 타입은 `strict`. `any`를 쓰지 않는다.
- 화면은 세로(폰) 기준에서 가로 무대(880×400)로 옮겨 왔다. 레이아웃을 건드리면 두 비율 모두 확인한다.

## 커밋

`분류: 내용` 형식의 한국어 한 줄. 분류는 실제로 쓰이는 것만: `코어:`, `웹:`, `밸런스 N차:`, `문서:`, `전투:`, `편성:`. 분류 없이 내용만 쓴 커밋도 있다. 한 줄에 변경을 쉼표로 나열하는 것이 이 저장소의 관례다.

**커밋·푸시는 사용자가 시킬 때만 한다.** 기능을 다 만들고 타입체크·빌드·QA 가 끝났어도 알아서 커밋하지 않는다 — 워킹 트리에 둔 채로 보고하고 기다린다 (2026-09-16 지적).

배포는 `main` push → GitHub Actions → Pages 자동. `npm run deploy`는 수동 백업 경로다. **배포는 시키지 않으면 하지 않는다.**

## 도구 분담 (Claude = Max, Codex = Plus)

할당량이 비대칭이고, **2026-09-17부터 Codex 잔량이 부족해 Claude 가 디자인까지 맡는다.** Codex 는 큰 리뉴얼에만 부른다.

- **Claude**: 규칙·밸런스·시뮬, 큰 리팩터링, 테스트, `docs/` 동기화, 한국어 텍스트와 고증 검토, **그리고 화면 전반** — `style.css`, 캔버스 그림(`scenes.ts`·`portrait.ts`·`stickman.ts`), 전투 연출(`battle-view.ts`), 카드·칩 배치, 효과음(`sound.ts`).
- **Codex**: **큰 리뉴얼만.** 화면 전체를 다시 그리는 수준의 개편, 아트 방향 재설정. 여력이 남으면 되돌리기 비싼 변경의 2차 리뷰(`codex review`).

**옛 규칙(2026-09-16 하루 동안): 디자인·CSS 는 Codex 담당.** 그때 Codex 가 별도 워크트리(`/private/tmp/lanista-design-main`)에서 작업하다 main 으로 옮겨 왔고, 남겨진 미커밋 작업을 Claude 가 합쳤다. 지금은 그 워크트리를 쓰지 않는다.

**디자인을 고칠 때 지킬 것** — 폼페이 벽 낙서풍(거친 선, 흙·검댕·주사 팔레트)을 벗어나지 않는다. 새 색을 들일 때는 이미 있는 팔레트에서 고른다. 화면은 세로(폰 390×844) 기준이고, 무대 배율(`--stage-k`)을 쓰는 곳은 화면 픽셀과 무대 좌표를 섞지 않는다. 임시로 넣은 스타일에는 주석으로 그 사실을 남긴다.

## 테스트

골든 테스트가 `src/core/*.test.ts` 에 있다 (`npm test`, node:test + tsx — 러너를 위해 의존성을 늘리지 않았다). 시드 고정 전투·새 게임·저장 왕복·몸 상태를 잠근다. **규칙이나 수치를 바꾸면 여기가 먼저 깨진다** — 의도한 변화면 테스트의 골든 값을 갱신하고 `docs/06-prototype.md` 에 근거를 남긴다. 화면은 아직 테스트가 없다.

## 지금 없는 것

화면(`src/web/`) 테스트가 0개다. 캔버스와 DOM 이라 골든 테스트가 잘 붙지 않는다 — 지금은 브라우저 QA(스크린샷 루프)로 대신한다.
